apiVersion: v1
kind: Service
metadata:
  name: threat-intel-api
  namespace: threat-intel
  labels:
    app: threat-intel-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: threat-intel-api
